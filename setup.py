from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='it is a journey project of keeping track of what I learn on Python',
    author='matt mercan',
    license='MIT',
)
